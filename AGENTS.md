# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# Linguagem

Este projeto é em JavaScript, não TypeScript. Não crie arquivos `.ts`/`.tsx` nem `tsconfig.json`.
